<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:Jlib.Controls;assembly=Jlib.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style TargetType="{x:Type controls:CameraStatusControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:CameraStatusControl}">
                    <Grid>
                        <!-- 灰色摄像头（断开） -->
                        <Image x:Name="GrayIcon" Source="/Theme.Default;component/Pictures/Camera2.png" Width="48" Height="30" />
                        <!-- 绿色摄像头（连接） -->
                        <Image x:Name="GreenIcon" Source="/Theme.Default;component/Pictures/Camera1.png" Width="48" Height="30" />
                        <!-- 中间闪烁的三角形 -->
                        <Polygon Points="14,25 14,5 27,15"
                             Fill="LightGoldenrodYellow"
                             Opacity="1">
                            <Polygon.Triggers>
                                <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                    <BeginStoryboard>
                                        <Storyboard RepeatBehavior="Forever" AutoReverse="True">
                                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                         From="1.0" To="0.3"
                                                         Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </EventTrigger>
                            </Polygon.Triggers>
                        </Polygon>
                    </Grid>

                    <!-- 切换图标显示 -->
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsConnected" Value="True">
                            <Setter TargetName="GreenIcon" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="GrayIcon" Property="Visibility" Value="Collapsed"/>
                        </Trigger>
                        <Trigger Property="IsConnected" Value="False">
                            <Setter TargetName="GreenIcon" Property="Visibility" Value="Collapsed"/>
                            <Setter TargetName="GrayIcon" Property="Visibility" Value="Visible"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>