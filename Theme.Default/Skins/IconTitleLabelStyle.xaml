<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:controls="clr-namespace:Jlib.Controls;assembly=Jlib.Controls"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Style x:Key="IconTitleLabelStyle" TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="#D9D9D9"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Padding" Value="5"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Label}">
                    <StackPanel Orientation="Horizontal" >
                        <Image Width="52" Height="53" Source="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.IconSource)}"  />
                        <Label Foreground="#D9D9D9" HorizontalContentAlignment="Left" VerticalContentAlignment="Center" FontWeight="Bold" Margin="20,0" 
                                   Content="{Binding RelativeSource={RelativeSource TemplatedParent},  Path=(controls:ControlHelper.TitleText)}"/>
                        <ContentPresenter  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>